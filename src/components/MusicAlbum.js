class MusicAlbum {
    constructor(albumName, performerName, genre, yearPublished, coverURL) {
        this.albumName = albumName;
        this.performerName = performerName;
        this.genre = genre;
        this.yearPublished = yearPublished;
        this.coverURL = coverURL;
        }
    }

export default MusicAlbum
